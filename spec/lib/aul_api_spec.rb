require 'aul_api'

describe AutoUplink do

	it "is a subclass of Mechanize" do
		expect(AutoUplink.new).to be_a(Mechanize)
	end

	it "can log into AUL given working credentials"

	it "can locate a vehicle, once logged in"

end